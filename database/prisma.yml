# The endpoint of your Prisma API (deployed to a Prisma Sandbox).
#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: http://10.14.10.94:4466

# The file containing the definition of your data model.
datamodel: datamodel.graphql
